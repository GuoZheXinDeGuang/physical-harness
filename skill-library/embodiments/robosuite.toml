# Abstract skill -> robosuite Stack persistent-segment task. Both segments are
# implemented by one stateful driver over one live Stack environment.
[skills.pick]
backend = "scripted_tabletop_stack"
task_template = "grasp_{object}"

[skills.place_on]
backend = "scripted_tabletop_stack"
task_template = "place_{object}_on_{target}"
