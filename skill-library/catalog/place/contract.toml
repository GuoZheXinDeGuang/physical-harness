[skill]
name = "place"
description = "Release a held object inside a named receptacle."
kind = "segment"
requires = ["robot is holding object", "target is reachable"]
ensures = ["object is inside target", "gripper has released object"]
exit_conditions = ["benchmark containment predicate and release predicate are true"]

[arguments]
object = "str"
target = "str"
