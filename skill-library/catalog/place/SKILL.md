---
name: place
description: Release a held object inside a named receptacle.
---

# Place

Both `object` and `target` must be grounded in the scene inventory. Success is
the benchmark's containment predicate plus release, not the policy saying done.
Measured evidence for the RoboCasa binding of this skill (kitchen_thaw) lives in
the capability-record store, not here -- see `skill-library/embodiments/robocasa.toml`.
