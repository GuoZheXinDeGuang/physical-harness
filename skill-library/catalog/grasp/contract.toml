[skill]
name = "grasp"
description = "Grasp and securely lift one named object."
kind = "segment"
requires = ["object is reachable", "gripper is free"]
ensures = ["robot is securely holding object"]
exit_conditions = ["backend grasp predicate is true and the object has lifted"]

[arguments]
object = "str"
