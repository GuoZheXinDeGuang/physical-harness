# 今夜目标（2026-08-25，操作员离场 10 小时）

操作员原话立此存照：

> 1. 我需要每天看到一个长链路的复杂的包含不止 manipulation 的任务可以在 sim 里执行，
>    这个任务最少需要 10 步，并且任务过程流和过程图需要是复杂的，需要是能够一步步执行
>    中间步骤如果错误需要是可以 replan 的，你可以结合 rsi 去做去创造小的技能去组合去
>    优化 harness，但是不要为了一个 pipeline 去 hardcode 一个 harness，需要是 rsi 的
>    方式去学习和制作，我们每天需要回来能看到你做到一个长链任务，遵循第一性原理。
> 2. 第二队 agent 去做 uiux 的优化，需要对抗式的多次优化迭代，需要最符合开发者的角度
>    去观看的使用的 uiux，并且继续提升 skill graph，任务图谱和过程流的展示，可以多
>    使用外部的包。

## 队伍一：M6 长链异构任务（≥10 步）

- 任务必须异构：不止抓放——感知/清点/决策/验证类节点与操纵节点混编，≥10 节点。
- 底座只长**通用**节点类（任何任务可复用），任务本身住在卡里——严禁为单管线特调底座。
- 逐步执行、节点失败触发 replan；全程在 UI 过程流/图谱可见。
- RSI 路径：链电池找最弱阶段 → campaign 学小技能 → 治理组合。证据纪律一条不破
  （账本预登记、标定不当门、held-out 烧一次、诚实空结果合法）。
- 每日交付物：一条可在 sim 复跑的长链任务 + 当日战报。

## 队伍二：UIUX 对抗迭代

- 先吸收在飞三修（连接竞态 / 图谱横排 / 任务台垂直拖拽）。
- 对抗循环 ×N：设计师审计 → 实现 → 冷眼验收，开发者视角优先。
- 重点面：skill graph、任务图谱、过程流展示。外部包放开（MIT/Apache，署名入册）。

## 夜规

老规矩不变：每步提交实时推送、base-gate 快照同 commit、封存 parity 不动、
执行态运行时不倒、:3081 始终可用、诚实汇报。
