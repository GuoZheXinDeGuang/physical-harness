"""RSI workload: the Verify -> Adapt edge of the OS main loop, as a plugin.

Consumes ``embodiment.env`` / ``policy.driver`` / ``graph.skill`` through the
kernel and turns a governed campaign's promotions into measured
``SkillRecord``s -- see ``plugins.rsi.workload.run``, this package's only
public surface, and ARCHITECTURE.md's "RSI workload(plugins/rsi)" note.
"""
